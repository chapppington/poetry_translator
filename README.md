# Poetry Translator with Ollama

## Установка и запуск

1. Запустите контейнер:
```bash
docker-compose up -d
```

2. Дождитесь полной загрузки Ollama (проверьте логи):
```bash
docker-compose logs -f ollama
```

3. Загрузите модель (например, gpt-oss:20b):
```bash
docker exec -it ollama-gpt-oss-20b ollama pull gpt-oss:20b
```

4. Проверьте, что модель загружена:
```bash
docker exec -it ollama-gpt-oss-20b ollama list
```

5. Установите зависимости Python:
```bash
pip install -r requirements.txt
```

6. Используйте Python приложение:
```bash
# Простой запрос
python app.py "Привет, как дела?"

# С указанием модели
python app.py --model gpt-oss:20b "Напиши стихотворение"

# Потоковый вывод
python app.py --stream "Расскажи историю"

# Список доступных моделей
python app.py --list
```

## Использование Python приложения

Приложение `app.py` предоставляет простой интерфейс для работы с Ollama:

- **Простой запрос**: `python app.py "ваш вопрос"`
- **Выбор модели**: `python app.py --model gpt-oss:20b "ваш вопрос"`
- **Потоковый вывод**: `python app.py --stream "ваш вопрос"`
- **Список моделей**: `python app.py --list`

## Увеличение памяти для больших моделей

Если модель требует больше памяти (например, `gpt-oss:20b` требует ~13GB), нужно увеличить лимит памяти в Docker Desktop:

1. Откройте **Docker Desktop**
2. Перейдите в **Settings** (Настройки)
3. Выберите **Resources** → **Advanced**
4. Увеличьте **Memory** до минимум **16GB** (или больше, если доступно)
5. Нажмите **Apply & Restart**

После этого перезапустите контейнер:
```bash
docker-compose down
docker-compose up -d
```

**Важно:** Убедитесь, что у вас достаточно RAM на Mac. Модель `gpt-oss:20b` требует минимум 13-14GB свободной памяти.

## Альтернатива: использование меньшей модели

Если у вас недостаточно памяти, используйте меньшую модель:
```bash
# Загрузите меньшую модель (например, 7B или 8B)
docker exec -it ollama-gpt-oss-20b ollama pull llama3:8b
python app.py --model llama3:8b "ваш запрос"
```

## ⚠️ Важно: GPU на macOS

**Ollama в Docker на macOS НЕ может использовать GPU (Metal).** Docker Desktop на macOS не имеет доступа к Metal API, поэтому все вычисления выполняются на CPU, что значительно медленнее.

### 🚀 Рекомендация: Использовать нативный Ollama для GPU

Для использования GPU (Metal) на Apple Silicon установите Ollama напрямую на macOS:

```bash
# Установка через Homebrew
brew install ollama

# Запуск Ollama
ollama serve

# В другом терминале загрузите модель
ollama pull gpt-oss:20b

# Используйте ваше Python приложение (оно будет работать с нативным Ollama)
python app.py "ваш запрос"
```

**Преимущества нативного Ollama:**
- ✅ Использует Metal (GPU) для ускорения
- ✅ В 3-10 раз быстрее, чем в Docker
- ✅ Меньше потребление памяти (GPU и CPU используют Unified Memory)
- ✅ Модель `gpt-oss:20b` будет работать намного быстрее

**Примечание:** Ваше Python приложение (`app.py`) будет работать с нативным Ollama без изменений, так как оно использует тот же API на `localhost:11434`.

## Примечания

- Модель будет сохранена в volume `ollama_data` (для Docker) или `~/.ollama` (для нативного)
- Конфигурация оптимизирована для macOS на ARM архитектуре (Apple Silicon)
- **Docker версия:** Использует только CPU (медленнее)
- **Нативная версия:** Использует Metal GPU (быстрее, рекомендуется)
- Docker-compose настроен на лимит 16GB памяти, но это работает только если Docker Desktop имеет достаточно выделенной памяти

